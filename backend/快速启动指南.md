# SignLink 后端快速启动指南

## 🚀 快速启动（3分钟）

### 1. 检查环境
```bash
# 检查Python版本（需要3.8+）
python --version
# 或
python3 --version

# 检查pip
pip --version
```

### 2. 进入后端目录
```bash
cd backend/
```

### 3. 安装依赖
```bash
# Windows
pip install -r requirements.txt

# macOS/Linux
pip3 install -r requirements.txt
```

### 4. 启动服务
```bash
# 方式1: 使用启动脚本
./start.sh

# 方式2: 直接启动
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### 5. 验证服务
打开浏览器访问：
- **API文档**: http://localhost:8000/docs
- **健康检查**: http://localhost:8000/api/health

## 📡 API测试

### 使用Python测试客户端
```bash
# 确保后端服务正在运行
python test_api.py
```

### 使用curl测试
```bash
# 健康检查
curl -X GET http://localhost:8000/api/health

# 获取模型信息
curl -X GET http://localhost:8000/api/model/info

# 获取支持的手语类别
curl -X GET http://localhost:8000/api/classes
```

### 使用Postman测试
1. 新建请求
2. URL: `http://localhost:8000/api/recognize/realtime`
3. Headers: `Content-Type: application/json`
4. Body (raw JSON):
```json
{
  "image": "data:image/jpeg;base64,/9j/4AAQSkZJRgABAQAAAQ...",
  "format": "jpeg",
  "quality": 80
}
```

## 📂 确保AI模型存在

### 检查模型文件
```bash
# 检查模型文件
ls -la ../ai_services/set_training_translation/sign_language_model.h5
ls -la ../ai_services/set_training_translation/sign_language_labels.json
```

### 如果模型不存在
```bash
# 进入AI服务目录
cd ../ai_services/set_training_translation/

# 按README中的步骤训练模型
python train_sign_language_model.py
```

## 🐛 常见问题

### 1. 端口被占用
```
Error: [Errno 10048] Only one usage of each socket address
```
**解决**: 关闭占用8000端口的程序，或更改端口
```bash
python -m uvicorn app.main:app --host 0.0.0.0 --port 8001
```

### 2. 模型加载失败
```
模型文件不存在: /path/to/model.h5
```
**解决**: 运行模型训练脚本生成模型文件

### 3. 依赖安装失败
```
ERROR: Could not install packages due to an OSError
```
**解决**: 升级pip或使用镜像
```bash
pip install --upgrade pip
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/
```

### 4. 权限错误（Linux/macOS）
```
Permission denied: './start.sh'
```
**解决**: 添加执行权限
```bash
chmod +x start.sh
```

### 5. 内存不足
```
OOM when allocating tensor
```
**解决**: 关闭其他程序，或使用更小的模型

## 📖 更多文档

- **详细文档**: [README.md](./README.md)
- **API接口**: http://localhost:8000/docs
- **项目主页**: https://github.com/Oxygen0604/SignLink

## 💬 获取帮助

- 📧 邮箱: [联系后端开发团队]
- 🐛 问题反馈: [GitHub Issues]
- 📖 开发文档: [项目Wiki]
